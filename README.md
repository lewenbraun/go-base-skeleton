# Go Base Skeleton

A Go project skeleton for agent-assisted development: a complete starting setup with agent instructions, a CLI entry point, standard project layout, tests, static analyzers, formatting, linting, and security tools. Use it to bootstrap a new project, then rename the `project` module and command for your application.
