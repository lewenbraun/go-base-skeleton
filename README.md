# Go Base Skeleton

A production-oriented Go project skeleton with a CLI entry point, standard project layout, and preconfigured formatting, linting, testing, and security tools. Use it as a starting point for new Go services, then rename the `project` module and command for your application.
